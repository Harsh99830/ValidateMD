/******** Tailwind v4-style config is optional when using @tailwindcss/vite. ********/
// This file provides a place to add any future custom theme/extensions if needed.
// With Tailwind v4 plugin for Vite, content paths are auto-detected via PostCSS-in-Vite.
// You can remove this file if you don't need customizations.

/** @type {import('tailwindcss').Config} */
export default {
  // Extend theme or plugins as needed
  theme: {
    extend: {}
  },
  plugins: []
}
